# ============================================
# Settings Service - Environment Variables
# ============================================
# IMPORTANT: All variables are injected from the root .env file via docker-compose
# This file is for DOCUMENTATION purposes only
# ============================================

# Service Configuration
PORT=8085
HOST=0.0.0.0
NODE_ENV=production

# Database Configuration
# Injected from root .env: DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}?schema=settings

# Redis Configuration
# Injected from root .env: REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/3

# API & CORS Configuration
# Injected from root .env: API_URL, CORS_ORIGINS, CORS_ORIGIN

# Logging
# Injected from root .env: LOG_LEVEL

# ============================================
# Required variables in root .env:
# ============================================
# POSTGRES_USER
# POSTGRES_PASSWORD
# POSTGRES_DB
# REDIS_PASSWORD
# CORS_ORIGINS
# API_URL
# LOG_LEVEL
# NODE_ENV
