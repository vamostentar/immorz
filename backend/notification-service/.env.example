# Notification Service Environment Variables

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/notifications?schema=notifications

# Redis (for BullMQ)
REDIS_HOST=localhost
REDIS_PORT=6379

# SMTP Configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@example.com
SMTP_PASS=your-password
SMTP_FROM=noreply@ribeirazul.com
SMTP_FROM_NAME=Ribeirazul Imobiliária

# Service
PORT=8086
NODE_ENV=development
LOG_LEVEL=info

# JWT (for auth validation)
JWT_SECRET=your-jwt-secret

# Internal Service URLs
AUTH_SERVICE_URL=http://localhost:8081
PROPERTIES_SERVICE_URL=http://localhost:8082
