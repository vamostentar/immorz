# ============================================
# Messages Service - Environment Variables
# ============================================
# IMPORTANT: All variables are injected from the root .env file via docker-compose
# This file is for DOCUMENTATION purposes only
# ============================================

# Service Configuration
PORT=8090
HOST=0.0.0.0
NODE_ENV=production

# Database Configuration
# Injected from root .env: DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}?schema=messages

# Email Configuration (SMTP)
# Injected from root .env:
#   SMTP_HOST
#   SMTP_PORT
#   SMTP_SECURE
#   AUTH_SMTP_USER
#   AUTH_SMTP_PASS
#   AUTH_EMAIL_FROM
#   SMTP_VERIFY_CONNECTION

# IMAP Configuration (Incoming Emails)
# Injected from root .env:
#   IMAP_ENABLED
#   IMAP_HOST
#   IMAP_PORT
#   IMAP_SECURE
#   IMAP_USER
#   IMAP_PASS
#   IMAP_POLL_INTERVAL

# Logging
# Injected from root .env: LOG_LEVEL

# ============================================
# Required variables in root .env:
# ============================================
# POSTGRES_USER
# POSTGRES_PASSWORD
# POSTGRES_DB
# SMTP_HOST
# SMTP_PORT
# SMTP_SECURE
# AUTH_SMTP_USER
# AUTH_SMTP_PASS
# AUTH_EMAIL_FROM
# IMAP_HOST
# IMAP_PORT
# IMAP_SECURE
# IMAP_USER
# IMAP_PASS
# LOG_LEVEL
# NODE_ENV
