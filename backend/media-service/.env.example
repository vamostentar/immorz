# ============================================
# Media Service - Environment Variables
# ============================================
# IMPORTANT: All variables are injected from the root .env file via docker-compose
# This file is for DOCUMENTATION purposes only
# ============================================

# Service Configuration
PORT=8083
HOST=0.0.0.0
NODE_ENV=production

# Redis Configuration
# Injected from root .env: REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/2

# MinIO / S3 Configuration
# Injected from root .env: 
#   S3_ENDPOINT=http://minio:9000
#   S3_ACCESS_KEY_ID
#   S3_SECRET_ACCESS_KEY
#   S3_BUCKET_NAME

# API & CORS Configuration
# Injected from root .env: API_URL, CORS_ORIGINS, CORS_ORIGIN

# Logging
# Injected from root .env: LOG_LEVEL

# ============================================
# Required variables in root .env:
# ============================================
# REDIS_PASSWORD
# S3_ACCESS_KEY_ID
# S3_SECRET_ACCESS_KEY
# S3_BUCKET_NAME
# CORS_ORIGINS
# API_URL
# LOG_LEVEL
# NODE_ENV
