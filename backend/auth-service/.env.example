# ============================================
# Auth Service - Environment Variables Example
# ============================================
# ATENÇÃO: Este ficheiro é um exemplo. Copie para .env e preencha os valores.
# O ficheiro .env está no .gitignore e não será enviado para o GitHub.
# ============================================

# Environment
NODE_ENV=development
PORT=8084
HOST=0.0.0.0

# Database (obrigatório)
DATABASE_URL=postgresql://user:password@localhost:5432/auth_db

# JWT Configuration (obrigatório - gerar segredo forte para produção)
# Gerar com: openssl rand -base64 64
JWT_SECRET=your-very-long-and-secure-jwt-secret-at-least-64-characters-long
JWT_ACCESS_EXPIRY=4h
JWT_REFRESH_EXPIRY=7d

# Redis (opcional mas recomendado para produção)
# REDIS_URL=redis://localhost:6379
# REDIS_PASSWORD=

# ============================================
# Admin Seed Configuration
# ============================================
# Para criar um utilizador Admin automaticamente ao iniciar o serviço,
# descomente e preencha as 3 variáveis abaixo com valores reais.
# NUNCA use estas variáveis em produção com valores fracos!

# SEED_DEFAULT_ADMIN=true
# DEFAULT_ADMIN_EMAIL=admin@your-domain.pt
# DEFAULT_ADMIN_PASSWORD=YourVeryStrongPassword123!

# ============================================
# Email Configuration (para verificação de email)
# ============================================
# SMTP_HOST=smtp.your-provider.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-smtp-user
# SMTP_PASS=your-smtp-password
# EMAIL_FROM=noreply@your-domain.pt

# ============================================
# CORS Configuration
# ============================================
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# ============================================
# Security
# ============================================
# MAX_LOGIN_ATTEMPTS=5
# LOCKOUT_DURATION=900
# SESSION_TIMEOUT=86400
